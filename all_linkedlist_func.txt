#include<iostream>
using namespace std;
struct node{
    int data;
    node* next;
};
node *head=NULL;
void create(int value)
{
  node *temp;
  node *n=new node;
 n->data=value;
 n->next=NULL;
 if(head!=NULL)
 { temp->next=n;

    temp=n;
   
    
 }
else
{
    head=temp=n; 
}
}
void disp()
{
    node* temp=head;
    while(temp!=NULL)
    {
        cout<<temp->data<<"->";
        temp=temp->next;
    }
    cout<<"NULL"<<endl;
}
void insert(int value,int pos)
{
    node *n=new node;
    n->data=value;
    n->next=NULL;
    node *temp=head;
    int i=1;
    if(pos==1)
    {
        n->next=head;
      head=n;
    }
    else
    {
while(i<pos-1)
{
    temp=temp->next;
    i++;
}
n->next=temp->next;
temp->next=n;
}
}
void del(int pos)
{
node *t1,*t2;
t2=head;
if(pos==1)
{
    head=head->next;
    free(t2);
}
else
{
    int i=1;
    while(i<pos-1)
    {
        t2=t2->next;
    i++;
    }
   t1=t2->next;
   t2->next=t1->next;
   free(t1);
}
}
void getlen()
{
    int c=0;
    node* temp=head;
while(temp!=NULL)
{
    c++;
    temp=temp->next;
}
cout<<c;
}
void reverse()
{
    node *temp,*prev,*nxt;
    temp=nxt=head;
    prev=NULL;
    while(nxt!=NULL)
    {
        nxt=nxt->next;
        temp->next=prev;
        prev=temp;
        temp=nxt;

    }
    head=prev;
}
int main(){
        create(2);
        create(3);
        create(4);
        create(5);
        create(6);
        disp();
        int pos=1;
        int k;
       cin>>k;int i=0;
        //no of times to insert numbers
       while(k)
        {cin>>pos;
        
        insert(i,pos);
        disp();
        i++;
        cin>>k;
        }
       // del(pos);
       disp();
       // getlen();
       // cout<<endl;*/
        cout<<"reversed list"<<endl;
        //reverse();
        //disp();
    return 0;
}